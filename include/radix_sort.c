#include "radix_sort.h"
#include <math.h>


/*
    Returns the maximum value in the linked list

    ARGS:
        Node* head -> linked list head
    RET:
        maximum integer value
*/ 
int get_max(Node* head) {
    //TODO
}

/*
    Get the digit at a specific place (0 = ones, 1 = tens, etc.)

    ARGS:
        int val -> the number
        int place -> the digit place
    RET:
        digit at the given place
*/
int get_digit(int val, int place) {
    //TODO
}

/* Radix sort requires append and concant, consider universal functions*/

/*
    Recursively sort a linked list using radix sort (base-10)

    ARGS:
        Node* head -> linked list head
    RET:
        returns the head of the sorted list
*/
Node* radix_sort(Node* head) {
    //TODO
}